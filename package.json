{
  "name": "retrorecon",
  "private": true,
  "devDependencies": {
    "stylelint": "^15.0.0",
    "stylelint-config-standard": "^34.0.0",
    "stylelint-config-html": "^1.1.0",
    "glob": "^10.3.10"
  },
  "scripts": {
    "lint:css": "stylelint \"static/**/*.css\"",
    "lint:inline": "node check_inline_styles.js",
    "lint": "npm run lint:css && npm run lint:inline"
  }
}
